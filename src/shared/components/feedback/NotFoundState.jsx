import { UI_MESSAGES } from "../../constants/messages";

const NotFoundState = () => (
  <main role="main" aria-labelledby="not-found-title">
    <h1 id="not-found-title">{UI_MESSAGES.NOT_FOUND}</h1>
  </main>
);

export default NotFoundState;
